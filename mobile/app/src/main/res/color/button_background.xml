<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color cuando el botón está presionado -->
    <item android:color="@color/purple_pressed" android:state_pressed="true" />
    <!-- Color cuando el botón está deshabilitado -->
    <item android:color="@color/button_disabled" android:state_enabled="false" />
    <!-- Color normal -->
    <item android:color="@color/purple" />
</selector>
